[package]
name = "svloop"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = "2.10.0"
libc = "0.2.178"
rustix = {version = "1.1.2", features = [
  "event",
  "process",
  "time",
  "pipe",
  "fs",
  "stdio",
  "runtime",
] }
